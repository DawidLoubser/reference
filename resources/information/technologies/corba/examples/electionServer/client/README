Make sure that

  1. the name server has been started via, for example 
  	tnameserv -ORBInitialPort 2000
  
  2. the election server has been started via, for example
  	java -jar server-1.0-SNAPSHOT.jar -ORBInitialPort 2000

then run the client via
	java -jar target/client-1.0-SNAPSHOT.jar -ORBInitialPort 2000
